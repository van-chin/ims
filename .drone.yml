kind: pipeline
type: docker
name: build

clone:
  disable: true



steps:
  - name: greeting
    image: node
    commands:
      - git clone http://gitea.inlin.cloud.local/ims/ims.chin.ink.git
      - git version
      - echo hello world
      - echo ok sucess
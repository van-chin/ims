kind: pipeline
type: ssh
name: default

server:
  host: 127.0.0.1
  user: van
  password:
    from_secret: myhost


platform:
  os: linux
  arch: amd64

steps:
  - name: greeting
    commands:
      - echo hello world